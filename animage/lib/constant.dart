import 'dart:ui';

const Color accentColor = Color.fromARGB(255, 238, 136, 135);
const Color accentColorLight = Color.fromARGB(255, 236, 159, 158);
const Color accentColorDark = Color.fromARGB(255, 184, 97, 96);

const Color transparency = Color.fromARGB(0, 0, 0, 0);

const Color defaultTransparentGrey = Color.fromARGB(100, 0, 0, 0);

const String detailsPageRoute = '/detailsPage';

const String viewOriginalPageRoute = '/viewOriginal';

const String id = '_id';

const String appDirectoryName = 'Animage';

const String postShareUrl = 'https://yande.re/post/show';

const String home = 'home_page';
const String postDetailsPage = 'post_details_page';
const String viewOriginalPage = 'view_original_page';

const double quarterSpace = space1 / 4;
const double halfSpace = space1 / 2;
const double space1 = 16.0;
const double space2 = space1 * 2;
const double space3 = space1 * 3;

const loadingIconSize = 256.0;
const smallLoadingIconSize = 128.0;
